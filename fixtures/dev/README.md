# Development fixtures

Sample missions, wallets, evidence, and dashboard snapshots used when `DEVELOPMENT_MODE=1`.

These are **not** live mainnet credentials. Safe to commit. Safe to edit for UI demos.

| File | Used by |
|------|---------|
| `sample-evidence.json` | BFF `/api/evidence/mainnet` |
| `sample-snapshot.json` | BFF `/api/snapshot` |
| `sample-wallets.json` | Docs / future wallet UI |
| `sample-missions.json` | Docs / mission wizard demos |
