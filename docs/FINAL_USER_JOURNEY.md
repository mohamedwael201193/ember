# Final user journey

Ideal path for a new user of EMBER.

```text
LANDING
  ↓
What is EMBER?  (hero + story)
  ↓
How it works    (rescue + architecture)
  ↓
Try it safely   (Open console — certified story by default)
  ↓
Agent / MCP     (/agent)
  ↓
Connect Cursor / Claude
  ↓
Inspect → Validate → Safe test
  ↓
KeeperHub Run   (Org A canvas / Runs)
  ↓
EMBER Console   (Mission / Payments)
  ↓
Rescue → Proof
  ↓
DONE
```

## Production operation path

```text
User / agent
  ↓
KeeperHub MCP
  ↓
KeeperHub workflow
  ↓
Real onchain execution
  ↓
EMBER continuity / recovery
  ↓
Proof
```

## What the website actually does

| Action | Classification |
| --- | --- |
| Console / Mission / Payments / Rescue / Proofs / Health / Wallets | READ-ONLY (live health when enabled; payment cards from certified evidence or labeled fixtures) |
| Mission builder | CONFIGURATION — local draft only |
| Practice rescue | REAL dry-run to runtime (`dryRun: true`) with confirmation — no USDC |
| Agent / MCP page | Documentation + copy prompts — no writes |
| KeeperHub Run / smoke | Performed by the agent via KeeperHub MCP — not by the website |

## Message

KeeperHub executes.  
EMBER keeps the mission alive.
